# --- SERVER SETTINGS ---
jwt.secret=change-this-dev-secret-to-a-long-random-string-at-least-32-characters
server.address=0.0.0.0
server.port=8080

# --- DISABLED EXCLUSIONS (To turn JPA back ON) ---
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration
# spring.data.jpa.repositories.enabled=false

# --- LOCAL DATABASE CONNECTION ---
# Notice we added 'group16b' to the end of the URL
spring.datasource.url=jdbc:postgresql://localhost:5432/group16b
spring.datasource.username=postgres
# IMPORTANT: Change this to your actual local Postgres password!
spring.datasource.password=P#L7[kU)Q`1,MAfE 
spring.datasource.driver-class-name=org.postgresql.Driver

# --- HIBERNATE MAGIC ---
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true